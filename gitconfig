[include]
    path = ~/.gitconfigLocal
[alias]
	co = checkout
	ci = commit
	st = status -s
	br = branch
	ba = branch -a
	com = checkout master
  dn = diff --name-only
  dh = diff HEAD^ HEAD
  dhn = diff HEAD^ HEAD --name-only
  lg = log --graph --pretty=format:'%C(red black)%h%Creset -%C(yellow black)%d%Creset %<(50,trunc)%s %C(green black)(%cr) %Cresetby: %C(yellow black)%an' -n 8

[color]
	diff = auto
	status = auto
	branch = auto
[advice]
	statusHints = false
[core]
	excludesfile = /Users/mvillene/.gitignore_global
[user]
	email = matthew.villeneuve@gmail.com
	name = Matthew Villeneuve
